// RED PHASE STUB — Story 4.1a: useAnnounce hook wrapping announce() utility
// TODO(story-4.1a): implement useAnnounce hook for component-level announcements
// This stub exists only to satisfy Vite import resolution for ATDD test files.
// Replace entirely during GREEN phase implementation.

import { announce } from '@/features/review/utils/announce'

export function useAnnounce() {
  return { announce }
}
