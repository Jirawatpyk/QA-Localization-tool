name: Chaos Test

on:
  schedule:
    # Weekly: Sunday at 03:00 UTC
    - cron: '0 3 * * 0'
  workflow_dispatch:

jobs:
  chaos:
    name: AI Fallback & Infrastructure Chaos
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Actual chaos scenarios will be populated in Epic 3
      # - AI provider timeout simulation
      # - Supabase connection failure
      # - Inngest queue failure
      - name: Run chaos tests (placeholder)
        run: echo "Chaos test scenarios will be added in Epic 3"
